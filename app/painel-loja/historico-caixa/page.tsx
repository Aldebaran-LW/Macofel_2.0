import { HistoricoCaixaClient } from './historico-caixa-client';

export const metadata = {
  title: 'Histórico do caixa | Painel da loja',
};

export default function HistoricoCaixaPage() {
  return <HistoricoCaixaClient />;
}
